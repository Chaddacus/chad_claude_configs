---
policy_doc_kind: ralph_loop_runbook
classification: pointer
canonical_owner: /Users/chadsimon/.claude/standards/RALPH_LOOP_RUNBOOK.md
authority_level: procedural
in_verifier_scope: false
---

# Ralph Loop Runbook — moved

Canonical: `/Users/chadsimon/.claude/standards/RALPH_LOOP_RUNBOOK.md`

This was a full copy until 2026-07-27. The two copies drifted and **both ended
up wrong, in different places** — this one had the wrapper name right
(`claude_run`, not `codex_run`) but mistranslated the real-binary variable into
`CLAUDE_HOME`, while the standards copy kept the codex-era `CODEX_REAL_BIN`.
The truth is `CLAUDE_REAL_BIN` (`/Users/chadsimon/.claude/bin/claude_run:157`).
The standards copy also carried the LEGACY status block this one was missing.

Collapsed to a pointer so it cannot drift again. Read the canonical file.
